name: CI/CD Pipeline

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  sast:
    uses: hijacksecurity/SecurityAsAService/.github/workflows/templates/sast.yml@main
    secrets: inherit

  sca:
    uses: hijacksecurity/SecurityAsAService/.github/workflows/templates/sca.yml@main
    secrets: inherit