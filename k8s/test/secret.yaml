apiVersion: v1
kind: Secret
metadata:
  name: myapp-secret
  namespace: myapp
type: Opaque
stringData:
  # pragma: allowlist nextline secret
  SECRET_KEY: "your-test-secret-key-here"
  # pragma: allowlist nextline secret
  DATABASE_URL: "postgres://myapp:testpassword@postgres:5432/myapp_test"
